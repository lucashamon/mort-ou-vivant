# 🐳 Spécifications : Dockerisation

## 1. Objectif
Rendre l'application déployable n'importe où via **Docker**. L'image doit être la plus légère possible (optimisée pour la production).

## 2. Stratégie de Build (Multi-stage)
Nous utiliserons un **Multi-stage build** pour séparer l'environnement de construction de l'environnement d'exécution.

### Stage 1 : Build (`builder`)
*   **Image de base :** `node:18-alpine` (ou version récente LTS).
*   **Actions :**
    *   Installation des dépendances (`npm ci`).
    *   Compilation de l'app (`npm run build`).
    *   Le dossier de sortie est `dist/`.

### Stage 2 : Production (`runtime`)
*   **Image de base :** `nginx:alpine`.
*   **Actions :**
    *   Copie des fichiers statiques depuis le stage `builder` (`dist/`) vers le dossier par défaut de Nginx (`/usr/share/nginx/html`).
    *   Configuration personnalisée de Nginx si nécessaire (pour gérer le SPA fallback si on utilisait un routeur, mais ici c'est simple).

## 3. Fichiers Requis

### 3.1. `Dockerfile`
Doit être à la racine du projet.

### 3.2. `.dockerignore`
Indispensable pour ne pas copier `node_modules` ou des fichiers sensibles dans le contexte Docker.
*   Exclure : `node_modules`, `.git`, `.vscode`, `dist` (car on le rebuild), etc.

### 3.3. `nginx.conf` (Optionnel mais recommandé)
Pour une configuration propre (compression Gzip, headers de cache).

## 4. Commandes Standard
Le projet doit pouvoir être construit et lancé avec :
```bash
# Build
docker build -t mort-ou-vivant .

# Run (Port 8080)
docker run -p 8080:80 mort-ou-vivant
```

## 5. Critères d'acceptation
*   L'image finale doit faire moins de **50 Mo** (grâce à Alpine).
*   L'application doit être accessible sur `http://localhost:8080`.
*   Le build doit réussir sans erreur.
