# 📱 Spécifications UI : Responsive & Mobile First

## 1. Philosophie "Mobile First"
L'application est conçue prioritairement pour les smartphones. Le style CSS par défaut (sans media queries) cible les petits écrans. Les règles pour tablettes et ordinateurs de bureau sont ajoutées en surcouche uniquement si nécessaire.

### Objectifs
* **Usage à une main :** Les zones d'interaction (boutons) doivent être situées dans la "Thumb Zone" (bas de l'écran).
* **Lisibilité :** Les textes doivent être lisibles sans zoomer.
* **Fluidité :** Le layout doit s'adapter à toutes les hauteurs d'écran sans créer de scroll inutile (dans la mesure du possible).

## 2. Structure du Layout (Mise en page)

### 2.1. Le Conteneur Principal (`#app` ou `main`)
Pour éviter que le jeu ne s'étire disgracieusement sur un écran large (Desktop), nous utiliserons une stratégie de **"Conteneur Centré"**.

* **Mobile (défaut) :** Largeur 100%, Hauteur 100%.
* **Desktop (via Media Query) :**
    * Le conteneur est limité à une largeur fixe (ex: `maxWidth: 500px`).
    * Il est centré horizontalement et verticalement sur la page.
    * Une bordure ou une ombre portée est ajoutée pour délimiter l'interface.

### 2.2. Flexbox Vertical
L'interface utilise **Flexbox** en direction colonne (`flex-direction: column`) pour empiler les éléments naturellement :
1.  **Header :** (Score / Vies) -> `flex: 0` (hauteur fixe).
2.  **Carte Personnage :** (Image + Texte) -> `flex: 1` (prend tout l'espace disponible restant).
3.  **Contrôles :** (Boutons Mort/Vivant) -> `flex: 0` (hauteur fixe, collés en bas).

## 3. Gestion des Unités et Dimensions

### 3.1. Hauteur d'écran (Le piège du 100vh)
Sur mobile, la barre d'adresse du navigateur (Safari/Chrome) peut masquer le bas de l'application si on utilise `100vh`.
* **Solution :** Utiliser l'unité **`100dvh`** (Dynamic Viewport Height) qui tient compte de la barre d'adresse mobile.
* **Fallback :** Garder `100vh` pour les vieux navigateurs.

### 3.2. Images
Les photos des célébrités doivent s'adapter sans être déformées.
* **CSS :**
  ```css
  img {
    width: 100%;
    height: 100%;
    object-fit: cover; /* Recadre l'image pour remplir la zone sans déformer */
    object-position: center top; /* Focus sur le visage */
    border-radius: 8px; /* Esthétique */
  }